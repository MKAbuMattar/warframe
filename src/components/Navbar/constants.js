// Menu transition configuration
export const MENU_TRANSITION = {
  TIMEOUT: 500,
  DEFAULT_MENU: 'main',
};

// Menu types
export const MENU_TYPES = {
  PRIMARY: 'primary',
  SECONDARY: 'secondary',
};

// Item types
export const ITEM_TYPES = {
  LINK: 'link',
  SUBMENU: 'submenu',
};

// Common colors
export const COLORS = {
  WHITE: '#ffffff',
};
